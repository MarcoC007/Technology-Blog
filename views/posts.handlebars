{{> post-info post }}

{{#if loggedIn }}

<section class="comment-section">

    <form class="form-comments">
         
        <div>
        <textarea class="textarea-comment"></textarea>
        </div>

        <div>
            <button class="btn is-success" id="comment-button" type="submit">Add Comment</button>
        </div>
 
    </form>
    {{/if}}

    {{> comments post.comments}}

    {{#if loggedIn}}
    <script src="/public/js/comment.js"></script>
    {{/if}}
</section>